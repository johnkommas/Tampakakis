version: "3.9"
services:
  app:
    build: .
    image: tampakakis:latest
    container_name: tampakakis
    ports:
      - "8001:8001"
    volumes:
      - ./data:/app/data
    restart: unless-stopped